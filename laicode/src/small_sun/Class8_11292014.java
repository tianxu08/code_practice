package small_sun;

public class Class8_11292014 {
	/*
	 * Q1. Given an array with all positive integers, 
	 * determine whether there exists a contiguous sub­array in the array, 
	 * which sums to a target number x.
	 */
	
	/*
	 * Q2  Merge Overlapping Intervals ( = total length covered )
	 */
	
	/*
	 * Q3   Given a text txt (size = n) and a pattern pat (size = m), 
	 * write a function PatternMatch (char pat[], char txt[]) that prints all occurrences of pat[] in txt[].
	 *  You may assume that n > m.
	 */
	
	/*
	 * Q4 
	 * Given an array of numbers  in string type , 
	 * arrange them in a way that yields the largest value. For example. 
	 * For example, if the given numbers are {“54”, “546”, “648”, “88”}, 
	 * the arrangement “ 8864854654 ” gives the largest value.
	 */
	
	/*
	 * Q5 Print Nodes in Top View of Binary Tree
	 */
	
	
	
	
}
