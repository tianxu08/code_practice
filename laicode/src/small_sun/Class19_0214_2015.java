package small_sun;

public class Class19_0214_2015 {
	/*
	 * Q1 
	 * Given an array A[N] with all positive integers from [1...N]. 
	 * How to get an array B[N] such that B[i] represents how many elements A[j] 
	 * that are smaller than A[i] and j > i.
	 * For example, given A[N] = { 4, 1, 3, 2 }, we should get B[N] = { 3, 0, 1, 0 }. Requirement: Time = O(nlogn).
	 */
	
	/*
	 * Q2 
	 * Given a 2D array, in which each row is sorted in an ascending order and 
	 * each column is sorted in ascending order. 
	 * How to determine whether a target number x is in the array or not.
	 */
	
	/*
	 * Q3  given a 2D integer array, where every row is sorted in increasing order. 
	 * How to find a common element in all rows. 
	 * If there is no common element, then returns ­1.
	 */
	
	/*
	 * Q5  (L inkedIn): Get all possible combinations of factors that can multiply to a target number.
	 */
}
