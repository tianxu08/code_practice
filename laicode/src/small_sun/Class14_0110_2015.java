package small_sun;

public class Class14_0110_2015 {
	/*
	 * Q1 . There is an unsorted array  A[N+1]  with all numbers from 1 to n, 
	 * and there is only one duplicate number, try to find it.
	 */
	
	/*
	 * Q2
	 * 2SUM → 2Diff
	 * Given a sorted array, find a pair of elements whose difference is identical to a target number.
	 */
	
	/*
	 * Q3 
	 * if­else expression.
	 * Implement a solution to parse a ternary expression in string type into a binary tree.
	 */
	
	/*
	 * Q4 
	 * Sorting with 3 stacks. Given that all numbers are initially stored in one stack. Requirements :O(1) space.
	 */
	
	/*
	 * Q5 
	 * Find the smallest window in a string containing all characters of another string
	 * Given two strings s1 and s2, find the  shortest  substring in s1 containing all characters in s2.
	 */
	
	/*
	 * Q6
	 * Given B[N], how to restore the original array A[N].  Time = O(n^2) → (nlogn).
	 */
	
	
}
