package small_sun;

public class Class20_0221_2015 {
	/*
	 * Q1
	 * 
	 * G iven a string A ( such as “abcd ef”), we want to make the minimum number of 
	 * changes to convert A to another string B (such as “abcd fe”) (Note that, size of A == size of B). 
	 * The changes can be made are
	 * 1) swap(string[i], string[i+1]); for all i (0<= i< size­1)
	 * 2) swap(string[0], string[A.size()­1]]);
	 * Return the minimum number of changes needed to convert A to B
	 */
	
	/*
	 * Q2
	 *  Diagonal Sum of a Binary Tree
	 */
	
	/*
	 * Q3 
	 * G iven a binary tree, the path cost from the root node to each leaf node is defined to 
	 * be the number of levels that the leaf is on. For example, the node 5 has its path cost to be 3,
	 * and node 8 has its path cost to be 4. whose cost is > = k that go through it.
	 */
	
	/*
	 * Q4
	 * classical question: Find closest k points to origin.
	 */
	
	/*
	 * Q5
	 * Given a s orted array A[N], how many tuples of x, y, z such that x+y+z < result.
	 */
	
	/*
	 * Q5.2
	 * Given a n array of positive integers, try to find how many tuples that can form a valid triangle
	 */
	
	/*
	 * Q6
	 * Given a string, we can insert a t most one empty space between each pair of adjacent letters. 
	 * Print all p ermutations of strings after insertions of empty spaces.
	 */
	
	/*
	 * Q7
	 * Given a matrix where every element is either ‘0’ or ‘1’, find the largest subsquare surrounded by ‘1’.
	 */
	
	/*
	 * Q8 
	 *  iven an integer array, try to find a contiguous subarray 
	 *  such that the sum of the elements in the subarray is equal to a target number.
	 */
	
	
}
