package small_sun;

public class Class12_12272014 {
	/*
	 * Q1    Given a sorted string of chars with duplicated chars, return  all possible subsequence . 
	 * The solution set must not contain duplicate  subsequence .
	 * For example,
	 * string input = “ab1b2”;
	 * output =
	 */
	
	/*
	 * Q2 ( LinkedIn) : Get all possible combinations of factors that can multiply to a target number.
	 */
	
	/*
	 * Q3 Given an unsorted array with all 0 or 1s. Return the  longest  sub­array that contains equal numbers of 0s and 1s.
	 */
	
	/*
	 * Q4 Given two strings where first string may contain wild card characters and second string is a normal string. 
	 * Write a function that returns true if the two strings match. The following are allowed wild card characters in first string.
	 * ­­> Matches with 0 or more instances of any character or set of characters. ? ­­> Matches with any  one  character.
	 * string1 wild card
	 * string2 normal
	 */
	
	
}
