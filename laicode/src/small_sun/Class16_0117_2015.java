package small_sun;

public class Class16_0117_2015 {
	/*
	 * Q1 Max Diff SubArrays in an unsorted array
	 */
	
	/*
	 * Q2  2SUM in BST
	 */
	
	/*
	 * Q3 
	 * 
	 *   有个类似 UTF­8 的character set, 里面包含了不同的语言的characters. 每个语言有一个range,
	 *   对应此语言的character的idx 只在这个range中。 每个range 不一定相 邻, 
	 *   可能会有一些空间reserve for future use. For example,
	 */
	
	/*
	 * Q4  Given an array with integers, 
	 * determine whether the array contains a valid  postorder traversal sequence of a BST.
	 */
	
	/*
	 * Q5  Longest Bitonic Subsequence.
	 */
	
}
